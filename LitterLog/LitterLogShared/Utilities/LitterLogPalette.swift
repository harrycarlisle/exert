import SwiftUI

enum LitterLogPalette {
    /// Warm golden yellow with readable contrast for dark text overlays.
    static let pee = Color(red: 0.93, green: 0.75, blue: 0.22)
    /// Warm medium brown.
    static let poo = Color(red: 0.55, green: 0.36, blue: 0.22)
    /// Muted coral / emergency red.
    static let tried = Color(red: 0.86, green: 0.38, blue: 0.35)
    /// Subdued teal accent.
    static let accent = Color(red: 0.22, green: 0.52, blue: 0.55)
    static let destructive = Color(red: 0.78, green: 0.22, blue: 0.22)
}
